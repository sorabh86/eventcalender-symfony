{% extends 'base.html.twig' %}

{% block body %}
<div class="wrapper">
    <div class="card">
    <div class="card-header">
        <h2 class="card-title">Add Category</h2>
    </div>
    <div class="card-body">
    	<!-- generate form based on tables in database -->
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
</div>
</div>
{% endblock %}
